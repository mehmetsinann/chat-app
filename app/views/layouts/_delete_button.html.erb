<% if @current_user && @current_user.is_admin %>
  <div class="absolute right-1 border rounded-full hover:bg-gray-800 duration-200">
    <%= link_to image_tag("delete.png",:class => "w-6 h-6 p-1"),obj , data: {
      turbo_method: :delete,
      turbo_confirm: "Are you sure?"
    } %>
  </div>
<% end %>